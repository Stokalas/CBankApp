#ifndef HEADER_AUTHENTICATION
#define HEADER_AUTHENTICATION

FILE* authentication();

int userInput();

char* login();

int registerNew();

int checkIfUserExists(char* username);

#endif
